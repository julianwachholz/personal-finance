import React from "react";

interface ImportWizardProps {}

export const ImportWizard = (props: ImportWizardProps) => {
  return (
    <div>
      <p>Import wizard goes here.</p>
    </div>
  );
};

export default ImportWizard;
